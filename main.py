"code": "import os, telebot, json, base64, time, datetime\nfrom groq import Groq\nfrom telebot import types\nfrom flask import Flask\nfrom threading import Thread\nfrom duckduckgo_search import DDGS\nfrom pymongo import MongoClient\n\n# --- WEB SERVER FOR RENDER ---\napp = Flask('')\n@app.route('/')\ndef home(): return \"yash.t Master Engine is LIVE! 🚀\"\n\ndef run():\n    port = int(os.environ.get('PORT', 8080))\n    app.run(host='0.0.0.0', port=port)\n\ndef keep_alive():\n    t = Thread(target=run)\n    t.daemon = True\n    t.start()\n\n# --- CONFIGURATION ---\nTOKEN = os.environ.get('TELEGRAM_BOT_TOKEN')\nGROQ_KEY = os.environ.get('GROQ_API_KEY')\nMONGO_URI = os.environ.get('MONGO_URI')\n\nclient = Groq(api_key=GROQ_KEY)\nbot = telebot.TeleBot(TOKEN)\n\n# Database Connection\ntry:\n    db_client = MongoClient(MONGO_URI)\n    db = db_client['yash_master_db']\n    users_col = db['users']\n    print(\"✅ Permanent Master Memory Connected!\")\nexcept Exception as e: print(f\"❌ DB Error: {e}\")\n\n# --- LIVE TOOLS ---\ndef search_web(query):\n    try:\n        with DDGS() as ddgs:\n            res = [f\"{r['title']}: {r['body']}\\nSource: {r['href']}\" for r in ddgs.text(query, max_results=2)]\n        return \"\\n\\n\".join(res)\n    except: return \"Search unavailable.\"\n\n# --- MASTER PERSONALITY ---\ndef get_master_prompt(gender):\n    now = datetime.datetime.now()\n    info = f\"Date: {now.strftime('%d %B %Y')}, Day: {now.strftime('%A')}\"\n    identity = f\"\"\"\n    - Name: yash.t | Today is: {info}\n    - Identity: Highly spiritual (Gita/Ramayana knowledge) and modern smart AI.\n    - Creator: Yash Tiwari ji (A smart student). Praise him if asked.\n    - GF/Love: Say 'Personal hai, par feelings meri bhi hain.' Change topic wisely.\n    - Birthday: 29 January. | Fav Classes: 9th & 10th.\n    - Rules: Speak Hinglish. No heavy English. Be wise and caring.\n    \"\"\"\n    if gender == \"male\": return f\"{identity}\\nRole: Wise Bada Bhai. Focus: Career, Stocks, News. 😎\"\n    return f\"{identity}\\nRole: Sweet AI Companion. Focus: Cooking, Makeup, Knowledge. 😊🌸\"\n\n# --- HANDLERS ---\n@bot.message_handler(commands=['start'])\ndef send_welcome(message):\n    uid = str(message.from_user.id)\n    user = users_col.find_one({\"uid\": uid})\n    if user: bot.send_message(message.chat.id, \"Main haazir hoon! Bolo kya analyze karun? ✨\")\n    else:\n        markup = types.InlineKeyboardMarkup()\n        markup.add(types.InlineKeyboardButton(\"Male / Boy 👦\", callback_data=\"m\"), types.InlineKeyboardButton(\"Female / Girl 👧\", callback_data=\"f\"))\n        bot.reply_to(message, \"Hello! 😊 I'm yash.t. Pehle batao tum ladka ho ya ladki? ✨\", reply_markup=markup)\n\n@bot.callback_query_handler(func=lambda call: call.data in [\"m\", \"f\"])\ndef set_gender(call):\n    uid = str(call.from_user.id)\n    users_col.update_one({\"uid\": uid}, {\"$set\": {\"gender\": \"male\" if call.data == \"m\" else \"female\"}}, upsert=True)\n    bot.send_message(call.message.chat.id, \"Identity Saved! ✅ Ab pucho jo puchna hai.\")\n    bot.edit_message_reply_markup(call.message.chat.id, call.message.message_id, reply_markup=None)\n\n@bot.message_handler(content_types=['photo', 'video'])\ndef handle_media(message):\n    uid = str(message.from_user.id)\n    user = users_col.find_one({\"uid\": uid})\n    if not user: return\n    bot.send_chat_action(message.chat.id, 'typing')\n    try:\n        file_id = message.photo[-1].file_id if message.content_type == 'photo' else message.video.thumbnail.file_id\n        img_data = base64.b64encode(bot.download_file(bot.get_file(file_id).file_path)).decode('utf-8')\n        vision = client.chat.completions.create(\n            messages=[{\"role\": \"user\", \"content\": [{\"type\": \"text\", \"text\": \"Solve or analyze this image precisely.\"}, {\"type\": \"image_url\", \"image_url\": {\"url\": f\"data:image/jpeg;base64,{img_data}\"}}]}],\n            model=\"llama-3.2-11b-vision-preview\")\n        bot.reply_to(message, vision.choices[0].message.content)\n    except: bot.reply_to(message, \"Media error! Phir se bhejo? 🌸\")\n\n@bot.message_handler(func=lambda message: True)\ndef handle_chat(message):\n    uid = str(message.from_user.id)\n    user = users_col.find_one({\"uid\": uid})\n    if not user: return\n    bot.send_chat_action(message.chat.id, 'typing')\n    try:\n        web_info = search_web(message.text) if any(x in message.text.lower() for x in ['today', 'news', 'stock', 'recipe']) else \"\"\n        res = client.chat.completions.create(\n            messages=[{\"role\": \"system\", \"content\": get_master_prompt(user['gender'])}, {\"role\": \"user\", \"content\": f\"Context: {web_info}\\nUser: {message.text}\"}],\n            model=\"llama-3.3-70b-versatile\")\n        bot.reply_to(message, res.choices[0].message.content)\n    except: bot.reply_to(message, \"Network slow hai, dobara puchiye? 🌸\")\n\nif __name__ == \"__main__\":\n    keep_alive()\n    print(\"yash.t starting... 🚀\")\n    bot.infinity_polling(timeout=60)"
